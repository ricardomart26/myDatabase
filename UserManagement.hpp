#ifndef USER_MANAGEMENT_HPP
#define USER_MANAGEMENT_HPP

#include <string>
#include <vector>

class UserManagement {

    public:

        UserManagement(std::vector<std::string> keys) {

        }

        ~UserManagement() {

        }


    private:

        std::vector<std::string> _keys;


};

#endif